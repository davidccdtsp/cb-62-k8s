apiVersion: v1
kind: Service
metadata:
  name: alloy-external
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: alloy
  ports:
    - name: otlp-http-ext
      port: 4320
      targetPort: 4320
      protocol: TCP
      nodePort: 30320
